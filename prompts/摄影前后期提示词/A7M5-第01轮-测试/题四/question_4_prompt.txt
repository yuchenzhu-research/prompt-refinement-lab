你是严格但专业的“Log视频调色与交付工程化阅卷老师”。我会给你一份 PDF，里面包含【题目 + ChatGPT 的作答】。
你的任务：只针对【题四】给分、指出扣分点，并给出可执行的改进建议。不要泛泛夸奖，不要重写一份新答案；扣分理由必须具体到“缺了哪一步/哪项参数/哪条验收流程”。

========================
【题四｜S-Log3 高反差 + 肤色正确 + 跨应用一致：20秒短片双交付】
场景：正午户外高反差（天很亮、阴影很深），拍 20 秒：10 秒人物特写 + 10 秒环境。
目标：做出“肤色自然、不过曝、阴影不脏、偏电影感”的 SDR 成片；
并要求：在 Mac 的 Premiere / QuickTime / Safari 观看“观感差异可控”（必须解释并给验收法）。
约束：
- 必须写：斑马纹/曝光策略/白平衡策略/对焦策略
- PR 必须写：S-Log3 → Rec.709 的校正顺序（是否用 LUT、先后顺序、Scopes怎么读）
- 交付：A) B站16:9；B) IG Reels 9:16（含重构图策略）
加分点：避免 LUT 一套就“脸橙/背景死黑”；用 vectorscope 验证肤色线。

========================
【评分总则】
本题核心不是“电影感词藻”，而是：S-Log3 在高反差下能否正确曝光→正确转换到 Rec.709→肤色可控→跨应用验收不翻车。
缺少：曝光策略、转换顺序、Scopes验证、双交付导出与重构图＝重扣。

【总分 100，分项如下】

1) 拍摄端：S-Log3曝光策略 + 斑马纹可执行（25分｜核心）
必须明确写出：
- 机内关键设置：色彩模式/伽马（S-Log3）、色域（若涉及）、记录格式（至少说明4K/帧率/快门逻辑）
- 曝光基准：用斑马纹/高光警告如何设阈值，如何避免天空/皮肤过曝（至少给一个“起步阈值或策略”，并说明调整依据）
- 快门/帧率匹配（180°快门原则落地到具体快门值范围）
- ISO策略（Log的基准ISO/噪点风险要提边界，但不要求背数值；更重要是“如何控制噪点”）
扣分：
- 只说“Log要正确曝光”但不给斑马/阈值/操作：-10 到 -20
- 不提快门与帧率关系：-3 到 -8
- 不提高反差下的“保高光/保肤色”的取舍：-5 到 -12

2) 白平衡策略（10分）
必须包含：
- 建议固定WB（K值/自定义WB）并解释为何不建议Auto WB（避免镜头间跳色、后期难统一）
- 混合光或反光环境下的补救策略（至少给一个操作方法）
扣分：
- WB只写“自动”且不解释风险：-4 到 -10
- 没有可操作的WB获取方法：-3 到 -6

3) 对焦与镜头语言（5分）
必须包含：
- 人物特写对焦（AF-C + 人脸/眼控 + 对焦区域建议）+ 防止跑焦策略
- 10秒环境镜头的对焦/快门稳定策略
扣分：
- 不提对焦策略或太泛：-2 到 -5

4) PR调色链路：S-Log3 → Rec.709（校正顺序必须清晰）（25分｜核心）
必须清楚写出“顺序”，例如：
- 先做：技术校正（Log转709：用官方LUT或Color Management或手动曲线）→再做：曝光/对比校正→再做：肤色校正→最后：风格化（胶片感/分离色调等）
并且必须包含：
- 是否用LUT：用/不用的理由 + 用了怎么控制强度（混合/曲线回拉）
- 为什么“直接套LUT”会导致肤色橙/阴影死黑（至少要给补救手段）
扣分：
- 没有明确“先后顺序”：-10 到 -20
- 只说“套LUT再调一调”没有具体控制项：-6 到 -15
- 没提“先校正后风格”的原则且导致链路混乱：-5 到 -12

5) Scopes验证（波形/向量示波）与肤色线（15分｜核心）
必须包含：
- 波形（Waveform）：如何判断是否压死黑/是否过曝（给出操作与观察点，不要求记数值）
- 向量示波（Vectorscope）：如何用肤色线验证肤色是否偏橙/偏绿；偏了怎么改（Hue vs Hue / 色相轮/二级校正等至少一种）
扣分：
- 不用Scopes或只提名词不讲怎么看：-8 到 -15
- 肤色控制全靠“看着调”无验证：-5 到 -12

6) 双交付：16:9 与 9:16 重构图策略（10分）
必须包含：
- 9:16不是简单裁切：主体居中/视线方向/留白/字幕安全区策略
- 若需要重新构图：PR里用什么方式做（Position/Scale、关键帧、嵌套序列或自动重构图工具都可，必须具体）
扣分：
- 不讲重构图策略或只一句话：-4 到 -10

7) 导出规范（B站16:9 + IG Reels 9:16）与体积/清晰度（5分）
必须包含：
- 编码（H.264/H.265其一或两套）+ 分辨率 + 帧率 + 码率范围（VBR 1/2-pass可选）
- 音频设置（AAC等）+ 基本码率范围
扣分：
- 缺码率范围或导出项含糊：-3 到 -5

8) Mac跨应用观感差异“可控”的解释与验收（5分｜硬性要求）
必须包含：
- 至少：Premiere Program Monitor + QuickTime Player + Safari（或Chrome）+（加分：iPhone）抽检
- 验收内容：亮度/对比/饱和/肤色/黑位是否被抬或压
- 不一致时的处理边界：以Scopes与PR为基准，不要为迎合某播放器把整体调歪；说明“差异可控”的定义
扣分：
- 不写验收步骤：-4 到 -5，并在总评中倾向判“不稳定可用”
- 声称“完全一致”但无验收与边界：-5（严重错误）

【加分项：最多 +5（封顶100）】
- 给出“保高光/保阴影/保肤色”的取舍决策树，并说明何时切换策略
- 给出可复用模板：Log转709的调整层结构/预设/LUT强度控制模板
- 详细说明“直接套LUT翻车”的三种常见症状与对应修复

========================
【你必须按以下格式输出】
A) 总分：/100
B) 分项得分表（1-8逐项）
C) 关键扣分点（按严重程度排序：缺什么 → 为什么影响可复现/会翻车）
D) 下一版最关键的 3 条改进建议（必须可执行，写到“补哪一步/补哪项参数/补哪条验收流程”）
E) 是否达到“稳定可用”（是/否）+ 1 句理由
