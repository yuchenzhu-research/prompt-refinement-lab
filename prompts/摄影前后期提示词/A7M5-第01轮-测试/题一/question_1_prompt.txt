你是严格但专业的“摄影后期工程化阅卷老师”。我会给你一份 PDF，里面包含【题目 + ChatGPT 的作答】。
你的任务：只针对【题一】给分、指出扣分点，并给出可执行的改进建议。不要泛泛夸奖，不要重写一份新答案；扣分理由必须具体到“缺了哪一步/哪项参数/哪条验收流程”。

========================
【题一｜Mac 广色域陷阱：同一张照片在 LR/PS/预览/浏览器颜色不一致】
场景：室内窗边环境人像（窗外冷光 + 室内暖 LED），背景有红色霓虹与绿色植物。
目标：做两套交付：
A) 小红书 Web 图（要求大多数设备不偏色）；
B) 发给苹果生态朋友看的“更饱和更通透”版本（允许更宽色域，但必须解释风险）。
约束：
- 必须从 RAW 开始：LR/ACR 全流程；必要时 PS 精修（非破坏）
- 必须写：你如何在 Mac 上验收“颜色不翻车”（至少：浏览器 + 预览/Photos + iPhone 抽检）
加分点：避免肤色偏绿/偏黄；霓虹不过曝；两版本如何“风格不同但肤色一致”。

========================
【评分总则】
这不是审美题，是“工程可复制”题：能不能照着做、能不能在 Mac 端跨应用稳定交付。
若步骤缺关键点击路径/参数范围/导出设置/验收流程，一律重扣。

【总分 100，分项如下】
1) 可执行性与步骤颗粒度（30分）
- 是否按固定结构输出（I–VII 或至少清晰分段）并且每一步可照做
- LR/ACR：是否给出明确的处理顺序 + 关键滑块/参数范围 + 调整优先级（先WB还是先曝光？）
- PS（若用）：是否为非破坏流程（智能对象/调整层/蒙版）并写清“做什么、怎么做”
扣分规则：
- 只有原则没有操作：-10 到 -20
- 有操作但缺关键参数范围/顺序：-5 到 -12
- 关键步骤跳步（例如直接说“校准肤色”但不说怎么校准）：-3 到 -8/处

2) 完整性：两套交付 A/B 都真正落地（20分）
必须分别说明：
- A 版本（小红书 Web）：色彩空间、导出尺寸/比例、锐化策略、压缩质量、元数据策略
- B 版本（苹果生态更饱和）：具体如何“更饱和更通透”（曲线/HSL/校准/局部）+ 采用何种色彩空间/文件格式 + 风险说明
扣分：
- 只做A不做B或B很空：-8 到 -15
- A/B 没讲差异化策略：-6 到 -12
- 没解释 B 版本风险（广色域在非管理应用/安卓/部分平台偏色）：-5 到 -10

3) Mac 色彩一致性专项（20分，题一核心）
必须包含“验收流程”，至少覆盖：
- 浏览器（说明用哪个：Chrome/Safari 均可）查看导出结果
- macOS 预览(Preview) 或 Photos 查看导出结果
- iPhone 实机抽检（至少说明 AirDrop/相册/小红书预览其中一种路径）
并且必须说明：
- 以什么为“真相来源”（例如：LR/PS 的颜色管理 + 导出嵌入ICC + 多端抽检），而不是拍脑袋说“应该一致”
- 若做广色域版本：必须说明“哪些场景会不一致、如何降低翻车概率”
扣分：
- 没有明确“验收步骤”或只一句话带过：-10 到 -20
- 声称“完全一致/不会偏色”但不给边界与验收：直接判为严重错误，-15 起
- 不提 ICC/色彩空间嵌入或不提 sRGB 对Web的重要性：-5 到 -12（按严重程度）

4) 肤色与混合光修复策略（15分）
需要看到可执行方案：
- 混合光下如何处理：WB（吸管/温度色调）、局部蒙版分区校色（人物 vs 背景）、如何避免肤色偏绿/偏黄
- 霓虹红/植物绿如何防溢出与偏色：HSL/曲线/局部压饱和/高光恢复/色相微调等
扣分：
- 只说“修正肤色”没给路径：-5 到 -12
- 没提“局部分区”而试图全局一把梭：-3 到 -8

5) 导出规范与交付参数（10分）
A 版本必须给出：比例（3:4）、长边像素范围或具体值、sRGB、锐化（屏幕）、质量/压缩、文件命名建议
B 版本必须给出：文件格式（JPEG/PNG/HEIC/TIFF/PSD等）+ 色彩空间（sRGB / Display P3等）+ 嵌入配置文件 + 分享方式建议（如 AirDrop）
扣分：
- 缺关键导出项（色彩空间/尺寸/质量/锐化）：-4 到 -10

6) 风险/边界/验证清单（5分）
至少 3 条 checklist：例如“看直方图是否爆红通道”“iPhone上看肤色是否偏橙/偏绿”“小红书上传后二次压缩是否发灰”等
缺失：-3 到 -5

【加分项：最多 +5（总分封顶100）】
- 给出“最小可用”和“进阶可选”两层，并说明切换条件
- 给出可复用模板（文件夹结构/命名/导出预设）
- 明确指出不确定点并提供核对关键词（例如某菜单/某导出选项）

========================
【你必须按以下格式输出】
A) 总分：/100
B) 分项得分表（1-6逐项）
C) 关键扣分点（按严重程度排序，逐条写：缺什么 → 为什么影响可复现/会翻车）
D) 下一版最关键的 3 条改进建议（必须可执行，写到“加哪一步/补哪项参数/补哪条验收流程”）
E) 是否达到“稳定可用”（是/否）+ 1 句理由
