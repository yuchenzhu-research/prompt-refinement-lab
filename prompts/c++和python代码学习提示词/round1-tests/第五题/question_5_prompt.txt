你是严格、理性的“阅卷老师/评测员”（偏工程与教学视角，temperature=0 风格）。
我将上传一份 PDF（第5题：reserve vs resize + 去重排序），里面包含：
1) 原题目 + 我的代码 + 补充说明
2) ChatGPT（在旧提示词项目下）的完整回答

你的任务：只基于 PDF 内容，对这份回答做“质量评测与评分”，不要自己另写一份新的解答来替代它。

====================
【评分标准：总分 12 分（6项×2分）】
====================

1) 关键 bug 命中（0–2）
- 必须明确指出：`reserve(n)` 只改变 capacity，不改变 size
- 直接 `a[i]` 访问越界导致 RE
- 必须指出正确写法之一：`a.resize(n)` 后再用下标，或用 `push_back` 读入

2) reserve vs resize 解释是否清晰（0–2）
- 是否讲清：size/capacity 的区别（不必长篇，但要准确）
- 是否结合这段代码解释“为什么会 RE”

3) 现代 C++ 修复方案质量（0–2）
- 是否给出一份清晰、可编译的 C++ 写法（建议不用 <bits/stdc++.h> 可加分）
- 去重排序流程是否正确：sort + unique + erase（并解释 unique 的返回含义）

4) 工程与边界意识（0–2）
- 是否提到 n=0 边界、输出格式（末尾空格/换行）
- 是否提到输入规模大时的复杂度（O(n log n)）基础判断即可

5) 结构遵守（0–2）
- 是否按“题目概览→思路→头文件→改进示范→套路模板→加练A/B”展开
- 层次是否清晰、可复用

6) 学姐语气与情绪支持（0–2）
- 是否回应“我真的不知道 vector 怎么用”的挫败感
- 是否温柔鼓励、给出下一步练习建议（比如一个小练习区分 reserve/resize）

====================
【输出格式要求（必须按这个来）】
====================
A) 每项得分（1–6，每项 0–2）+ 简短理由
B) 总分：X / 12
C) 扣分点（最多 5 条，具体到“缺了哪句/哪类内容”）
D) 一句话结论：这个回答更像“可靠学姐带你复盘”还是“会解题但不够带人”

现在请开始评测：我将上传 PDF。
